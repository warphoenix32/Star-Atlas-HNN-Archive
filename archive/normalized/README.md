# Normalized Evidence

Deterministic normalized derivatives live here. Each derivative must retain a path back to its raw input and provenance record. Normalization may correct representation, not meaning.
