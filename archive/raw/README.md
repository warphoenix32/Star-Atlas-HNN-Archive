# Raw Evidence

Preserve source inputs here without silent correction. The Wave 1 normalized URL inventory’s original JSON input is under `manifests/`; future raw captures should retain original filenames, dates, and custody metadata.
