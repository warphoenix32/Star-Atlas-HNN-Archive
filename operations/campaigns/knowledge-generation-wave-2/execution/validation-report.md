# Wave 2A Validation Report

Result: **PASS WITH CURATOR-APPROVED SCOPE EXTENSION**

Validated 19 outputs: 12 created and 7 expanded. The campaign now contains 19 evidence packets. Risk distribution is 6 R1 and 13 R2; status distribution is 6 `CANONICAL`, 1 `HISTORICAL`, and 12 `QUALIFIED`.

The nineteenth output is `knowledge/Entity-Relationship-Map.md`. It was explicitly authorized after semantic review to provide a human-readable ontology and cross-domain navigation layer. Its addition exceeds the original 18-output ceiling by one without changing the risk policy or introducing R3–R5 content.

The map has a dedicated evidence packet, required metadata, resolvable internal links, and an incoming link from `knowledge/README.md`. It summarizes reviewed relationships without generating canonical graph triples.

No `archive/`, `archive/semantic/`, `graph/`, or `publication/` path is changed. Proposal, vote, passage, implementation, payment, and verification remain separate. Council operational claims retain the required attribution. No R3, R4, or R5 page is present.

The repository has no general schema or Markdown-lint command; the campaign validator parses campaign JSON, checks the controlled front-matter schema, and resolves internal Markdown links. The curator extension was reviewed manually and is recorded in both the machine-readable and human-readable reports.
